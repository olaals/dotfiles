# Default Profile Settings
function set_profile
    echo "Default profile applied"
end

function unset_profile
    # No need to reset anything for default profile
    echo "Default profile removed"
end
